const ADMIN = "admin";

const STUDENT = "student";

const sendGridEmailAddresses = {};

const YEAR = 2023;

const stripeProductsPrices = {};

const essayModes = {
  brainstorm: "brainstorm",
  outline: "outline",
};

module.exports = {
  ADMIN,
  STUDENT,
  sendGridEmailAddresses,
  stripeProductsPrices,
  YEAR,
  essayModes,
};
